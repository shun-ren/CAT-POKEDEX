{
  "name": "catdex-singapore",
  "version": "0.1.0",
  "private": true,
  "description": "A privacy-first pixel-art cat field guide for Singapore.",
  "scripts": {
    "build": "node scripts/verify-build.mjs",
    "start": "npx serve build"
  }
}
